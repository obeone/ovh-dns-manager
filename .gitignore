# --- Security (CRITICAL) ---
# Stores sensitive OVH API keys - NEVER commit this file to Git
.env

# --- Python ---
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual Environments (depending on what you named it)
venv/
env/
.venv/

# --- PyInstaller (Build Artifacts) ---
# Contains the temporary files used during the build process
build/
# Contains the final executable (.exe)
dist/
# PyInstaller spec file (generated automatically, usually safe to ignore)
*.spec

# --- IDE & OS Metadata ---
# VS Code settings
.vscode/
# PyCharm settings
.idea/
# macOS folder metadata
.DS_Store
# Windows thumbnail cache
Thumbs.db